version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - echo "Installing dependencies"
  build:
    commands:
      - echo "Building your HTML file"
      - npm run build
artifacts:
  files:
    - '**/*.*'
